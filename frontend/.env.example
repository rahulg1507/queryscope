# Leave blank to use same-origin /api requests (recommended for Docker Compose).
VITE_API_BASE_URL=

# For a separately hosted backend, set an explicit public origin, for example:
# VITE_API_BASE_URL=https://api.example.com
