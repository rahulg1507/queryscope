import type { Route } from '../routing'

export function RoadmapPage({ onNavigate }: { onNavigate: (route: Route) => void }) {
  const completed = ['SQL lexer and parser', 'AST generation', 'In-memory tables and SELECT execution', 'Execution plans and visualization', 'Nested-loop and hash joins', 'GROUP BY and aggregates', 'B+ tree indexes and index scans', 'Rule-based and cost-based optimization', 'Statistics and cardinality estimation', 'Deterministic performance lab', 'Developer workspace productization']
  const future = ['ORDER BY, LIMIT, DISTINCT, and richer expressions', 'Outer joins, multiple join chains, and subqueries', 'Mutations and durable persistence', 'Composite, unique, partial, and expression indexes', 'Join reordering, predicate pushdown, and richer statistics']
  return <main className="main-content roadmap-page"><div className="page-intro"><div><p className="section-kicker">PROJECT DIRECTION</p><h1>Roadmap</h1><p className="intro-copy">What is already usable in the learning workspace and what comes next.</p></div></div><div className="roadmap-grid"><section className="roadmap-column" aria-labelledby="completed-title"><div className="roadmap-heading completed"><span>✓</span><h2 id="completed-title">Completed</h2></div>{completed.map((item) => <div className="roadmap-item" key={item}><span className="roadmap-check">✓</span>{item}</div>)}</section><section className="roadmap-column" aria-labelledby="future-title"><div className="roadmap-heading future"><span>→</span><h2 id="future-title">Future work</h2></div>{future.map((item) => <div className="roadmap-item" key={item}><span className="roadmap-dot" />{item}</div>)}</section></div><button className="run-button roadmap-cta" type="button" onClick={() => onNavigate('/workspace')}>Open Workspace</button></main>
}
