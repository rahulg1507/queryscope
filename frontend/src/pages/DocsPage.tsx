import { Check, Copy, Play } from 'lucide-react'
import { useState } from 'react'
import type { Route } from '../routing'

type DocsPageProps = { onNavigate: (route: Route) => void }
const examples = [
  { title: 'Filtered projection', sql: 'SELECT name, age\nFROM users\nWHERE age > 18;' },
  { title: 'Equality join', sql: 'SELECT users.name, expenses.amount\nFROM users\nJOIN expenses\nON users.id = expenses.user_id;' },
  { title: 'Grouped aggregate', sql: 'SELECT user_id, SUM(amount)\nFROM expenses\nGROUP BY user_id;' },
]

export function DocsPage({ onNavigate }: DocsPageProps) {
  const [copied, setCopied] = useState('')
  async function copy(sql: string, title: string) {
    try { await navigator.clipboard?.writeText(sql) } catch { /* clipboard permissions are optional */ }
    setCopied(title)
    window.setTimeout(() => setCopied(''), 1200)
  }
  return <main className="main-content docs-page"><div className="page-intro"><div><p className="section-kicker">QUERY SCOPE GUIDE</p><h1>Docs</h1><p className="intro-copy">A practical guide to the current educational SQL engine and its visual workspace.</p></div><div className="version-badge">SUPPORTED SQL · 1.0</div></div><div className="docs-layout"><aside className="docs-toc" aria-label="Documentation sections"><strong>On this page</strong>{['Quick start', 'SQL support', 'Lexer and parser', 'AST', 'In-memory storage', 'Execution', 'Joins', 'Aggregates', 'Indexes', 'Optimizer', 'Statistics', 'Benchmarks', 'Plans and metrics', 'Schema explorer', 'Limitations'].map((item) => <a key={item} href={`#${item.toLowerCase().replaceAll(' ', '-')}`}>{item}</a>)}</aside><div className="docs-content"><DocSection id="quick-start" title="Quick start"><p>Start the backend and frontend, open Workspace, then load an example or enter SQL. Parse shows the immutable AST; Run query shows result rows, actual metrics, and the generated physical plan.</p></DocSection><DocSection id="sql-support" title="SQL support"><p>QueryScope supports SELECT statements with named or wildcard projections, FROM, one equality-based inner JOIN, WHERE comparisons, GROUP BY, COUNT, SUM, AVG, integer/string/boolean literals, and optional trailing semicolons.</p></DocSection><DocSection id="lexer-and-parser" title="Lexer and parser"><p>The lexer is case-insensitive for supported keywords and produces tokens for identifiers, literals, operators, punctuation, and comments. The parser validates the supported grammar and returns clear HTTP 400 messages for invalid SQL.</p></DocSection><DocSection id="ast" title="AST"><p>Parsing produces an immutable-style JSON AST with SELECT columns, source table, optional join, predicate, grouping expressions, and aggregate expressions. The workspace renders it as an expandable tree.</p></DocSection><DocSection id="in-memory-storage" title="In-memory storage"><p>The demo database seeds users and expenses when the backend starts. Data is intentionally reset on restart; there is no persistence or mutation UI yet.</p></DocSection><DocSection id="execution" title="Execution"><p>Execution resolves columns, validates types, applies filters and projections, and returns rows plus actual rows-scanned and rows-returned metrics. Auto mode selects eligible physical strategies deterministically.</p></DocSection><DocSection id="joins" title="Joins"><p>Equality inner joins support nested-loop and hash execution. Manual mode lets you compare both strategies; qualified names such as users.id and expenses.user_id avoid ambiguity.</p></DocSection><DocSection id="aggregates" title="Aggregates"><p>GROUP BY supports COUNT(*), COUNT(column), SUM(integer_column), and AVG(integer_column). Group order is deterministic and every selected non-aggregate column must appear in GROUP BY.</p></DocSection><DocSection id="indexes" title="Indexes"><p>The Schema Explorer can create one-column order-4 B+ tree indexes on integer or string columns. Matching equality and range predicates can use an index scan.</p></DocSection><DocSection id="optimizer" title="Optimizer"><p>AUTO mode compares deterministic abstract costs for eligible table/index scans and nested-loop/hash joins. MANUAL mode preserves the selected strategies. The optimizer panel explains candidates, estimates, and rules.</p></DocSection><DocSection id="statistics" title="Statistics"><p>Statistics include table row counts, per-column distinct values and numeric ranges, and index distinct keys. They are refreshed as a consistent snapshot for optimization requests.</p></DocSection><DocSection id="benchmarks" title="Benchmarks"><p>Benchmarks use isolated deterministic datasets and compare operation counts, not wall-clock timing. Open Benchmarks for predefined index, join, and optimizer scenarios.</p></DocSection><DocSection id="plans-and-metrics" title="Plans and metrics"><p>Plans are trees containing operators such as PROJECTION, FILTER, AGGREGATE, TABLE_SCAN, INDEX_SCAN, NESTED_LOOP_JOIN, and HASH_JOIN. Estimated optimizer values are shown separately from actual execution metrics.</p></DocSection><DocSection id="schema-explorer" title="Schema explorer"><p>Workspace lists tables, columns, indexes, and safe row-count statistics. Expand a table to inspect details, use its name in the editor, refresh the catalog, or create an index.</p></DocSection><DocSection id="limitations" title="Limitations"><p>LEFT/RIGHT/FULL/CROSS/NATURAL JOIN, multiple join chains, USING, non-equality joins, subqueries, HAVING, ORDER BY, LIMIT, DISTINCT, NULL semantics, mutations, persistence, and advanced indexes remain future work.</p></DocSection><section className="docs-section" aria-labelledby="examples-title"><div className="docs-section-heading"><p className="section-kicker">COPY OR LOAD</p><h2 id="examples-title">Supported examples</h2></div><div className="docs-examples">{examples.map((example) => <article className="docs-example" key={example.title}><strong>{example.title}</strong><pre>{example.sql}</pre><div><button className="secondary-button" type="button" onClick={() => void copy(example.sql, example.title)}>{copied === example.title ? <Check size={14} /> : <Copy size={14} />} {copied === example.title ? 'Copied' : 'Copy'}</button><button className="secondary-button" type="button" onClick={() => { localStorage.setItem('queryscope.pendingQuery', example.sql); onNavigate('/workspace') }}><Play size={14} /> Use in Workspace</button></div></article>)}</div></section></div></div></main>
}

function DocSection({ id, title, children }: { id: string; title: string; children: React.ReactNode }) { return <section className="docs-section" id={id}><h2>{title}</h2>{children}</section> }
