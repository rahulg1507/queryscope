import { Check, Copy, Play } from 'lucide-react'
import { useState, type ReactNode } from 'react'
import type { Route } from '../routing'

type DocsPageProps = { onNavigate: (route: Route) => void }
const examples = [
  { title: 'Filtered projection', sql: 'SELECT name, age\nFROM users\nWHERE age > 18;' },
  { title: 'Equality join', sql: 'SELECT users.name, expenses.amount\nFROM users\nJOIN expenses\nON users.id = expenses.user_id;' },
  { title: 'Grouped aggregate', sql: 'SELECT user_id, SUM(amount)\nFROM expenses\nGROUP BY user_id;' },
]

const sections = [
  ['overview', 'Overview'], ['what-is-a-database', 'What is a database?'], ['what-is-sql', 'What is SQL?'], ['how-queryscope-works', 'How QueryScope works'], ['quick-start', 'Quick start'], ['sql-support', 'SQL support'], ['execution-plans', 'Execution plans'], ['table-scans', 'Table scans'], ['indexes', 'Indexes'], ['joins', 'Joins'], ['aggregation', 'Aggregation'], ['optimizer', 'Optimizer'], ['benchmarks', 'Benchmarks'], ['limitations', 'Limitations'], ['schema-explorer', 'Schema explorer'],
] as const

export function DocsPage({ onNavigate }: DocsPageProps) {
  const [copied, setCopied] = useState('')
  async function copy(sql: string, title: string) {
    try { await navigator.clipboard?.writeText(sql) } catch { /* clipboard permissions are optional */ }
    setCopied(title)
    window.setTimeout(() => setCopied(''), 1200)
  }
  return <main className="main-content docs-page"><div className="page-intro"><div><p className="section-kicker">QUERY SCOPE GUIDE</p><h1>Docs</h1><p className="intro-copy">Understand SQL, execution plans, and the small relational engine behind this workspace.</p></div><div className="version-badge">DEVELOPER PREVIEW</div></div><div className="docs-layout"><aside className="docs-toc" aria-label="Documentation sections"><strong>On this page</strong>{sections.map(([id, title]) => <a key={id} href={`#${id}`}>{title}</a>)}</aside><div className="docs-content"><DocSection id="overview" title="Overview"><p>QueryScope is a small, educational relational database engine with a workspace that makes parsing, planning, and execution visible. It connects SQL text to the work a database performs.</p></DocSection><DocSection id="what-is-a-database" title="What is a database?"><p>A database organizes information so it can be searched and combined reliably. QueryScope uses a small in-memory database containing demo users and expenses, so you can explore database behavior without setting up a production data store.</p></DocSection><DocSection id="what-is-sql" title="What is SQL?"><p>SQL is the language used to ask a database questions. A SELECT query describes which columns to return, which table to read, and which rows to filter.</p></DocSection><DocSection id="how-queryscope-works" title="How QueryScope works"><p>Write or load SQL, parse it into an AST, build an execution plan, optimize eligible operations, and execute against in-memory data. Each stage is represented in Workspace.</p></DocSection><DocSection id="quick-start" title="Quick start"><p>Choose or write SQL, select Parse to inspect the AST, select Run query to execute it, then inspect the results, plan, and optimizer decisions. Use Benchmarks to compare deterministic strategies.</p></DocSection><DocSection id="sql-support" title="SQL support"><p>Supported SQL includes SELECT, named or wildcard projections, FROM, one equality-based inner JOIN, WHERE comparisons, GROUP BY, COUNT, SUM, AVG, integer/string/boolean literals, and optional trailing semicolons.</p></DocSection><DocSection id="execution-plans" title="Execution plans"><p>A plan is the ordered work the engine performs. QueryScope displays PROJECTION, FILTER, AGGREGATE, TABLE_SCAN, INDEX_SCAN, NESTED_LOOP_JOIN, and HASH_JOIN operators with actual input and output rows.</p></DocSection><DocSection id="table-scans" title="Table scans"><p>A table scan checks rows directly from a table. It is the reliable fallback when no matching index is available.</p></DocSection><DocSection id="indexes" title="Indexes"><p>The Schema Explorer can create one-column B+ tree indexes on integer or string columns. Matching equality and range predicates can use an index scan.</p></DocSection><DocSection id="joins" title="Joins"><p>Equality inner joins support nested-loop and hash execution. Manual mode lets you compare both strategies, while qualified names such as users.id avoid ambiguity.</p></DocSection><DocSection id="aggregation" title="Aggregation"><p>GROUP BY supports COUNT(*), COUNT(column), SUM(integer_column), and AVG(integer_column). Group order is deterministic.</p></DocSection><DocSection id="optimizer" title="Optimizer"><p>AUTO mode compares deterministic abstract costs for eligible table/index scans and nested-loop/hash joins. MANUAL mode preserves selected strategies, and the optimizer panel explains candidates and estimates.</p></DocSection><DocSection id="benchmarks" title="Benchmarks"><p>Benchmarks use isolated deterministic datasets and compare operation counts, not wall-clock timing. Open Benchmarks for predefined scan, join, and optimizer scenarios.</p></DocSection><DocSection id="limitations" title="Limitations"><p>Outer joins, multiple join chains, subqueries, HAVING, ORDER BY, LIMIT, DISTINCT, NULL semantics, mutations, persistence, and advanced indexes remain future work.</p></DocSection><DocSection id="schema-explorer" title="Schema explorer"><p>Expand a table to inspect columns and types, use its name in the editor, refresh the catalog, or create an index. Loading an example never runs it automatically.</p></DocSection><section className="docs-section" aria-labelledby="examples-title"><div className="docs-section-heading"><p className="section-kicker">COPY OR LOAD</p><h2 id="examples-title">Supported examples</h2></div><div className="docs-examples">{examples.map((example) => <article className="docs-example" key={example.title}><strong>{example.title}</strong><pre>{example.sql}</pre><div><button className="secondary-button" type="button" onClick={() => void copy(example.sql, example.title)}>{copied === example.title ? <Check size={14} /> : <Copy size={14} />} {copied === example.title ? 'Copied' : 'Copy'}</button><button className="secondary-button" type="button" onClick={() => { localStorage.setItem('queryscope.pendingQuery', example.sql); onNavigate('/workspace') }}><Play size={14} /> Use in Workspace</button></div></article>)}</div></section></div></div></main>
}

function DocSection({ id, title, children }: { id: string; title: string; children: ReactNode }) { return <section className="docs-section" id={id}><h2>{title}</h2>{children}</section> }
